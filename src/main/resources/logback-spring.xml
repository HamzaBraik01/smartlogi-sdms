<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- === Définition du pattern d'affichage === -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- === Appender Console === -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- === Appender Fichier (optionnel) === -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/smartlogi.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/smartlogi-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- === Logger pour ton application === -->
    <logger name="com.smartlogi.sdms" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </logger>

    <!-- === Logger pour Spring (moins verbeux) === -->
    <logger name="org.springframework" level="WARN" />

    <!-- === Logger racine === -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>

</configuration>
