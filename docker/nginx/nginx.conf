# ===============================================
}
    # }
    #     }
    #         include /etc/nginx/proxy_params;
    #         proxy_pass http://sdms_api;
    #     location /actuator/health {
    #     # Health check
    #
    #     }
    #         include /etc/nginx/proxy_params;
    #         proxy_pass http://sdms_api;
    #     location /swagger-ui/ {
    #     # Swagger UI
    #
    #     }
    #         include /etc/nginx/proxy_params;
    #         proxy_pass http://sdms_api;
    #
    #         limit_conn conn_limit 5;
    #         limit_req zone=api_limit burst=5 nodelay;
    #     location /auth/ {
    #     # Auth endpoints (rate limiting plus strict)
    #
    #     }
    #         include /etc/nginx/proxy_params;
    #         proxy_pass http://sdms_api;
    #
    #         limit_conn conn_limit 10;
    #         limit_req zone=api_limit burst=20 nodelay;
    #     location /api/ {
    #     # API endpoints
    #
    #     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    #     add_header X-XSS-Protection "1; mode=block" always;
    #     add_header X-Content-Type-Options "nosniff" always;
    #     add_header X-Frame-Options "SAMEORIGIN" always;
    #     # Headers de sécurité
    #
    #     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    #     # HSTS
    #
    #     ssl_session_tickets off;
    #     ssl_session_timeout 1d;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers on;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     # Configuration SSL sécurisée
    #
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     ssl_certificate /etc/nginx/ssl/fullchain.pem;
    #     # Certificats SSL
    #
    #     server_name api.smartlogi.com;
    #     listen 443 ssl http2;
    # server {
    # Configuration HTTPS (décommenter et configurer en production)

    }
        }
            include /etc/nginx/proxy_params;
            proxy_pass http://sdms_api;
        location / {
        # En développement, proxy direct

        # return 301 https://$server_name$request_uri;
        # Redirection vers HTTPS (décommenter en production)

        }
            root /var/www/certbot;
        location /.well-known/acme-challenge/ {
        # Pour Let's Encrypt

        server_name api.smartlogi.com localhost;
        listen 80;
    server {
    # Redirection HTTP vers HTTPS

    }
        keepalive 32;
        server sdms-api:8080;
    upstream sdms_api {
    # Upstream pour l'API

    limit_conn_zone $binary_remote_addr zone=conn_limit:10m;
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    # Rate limiting

               application/xml application/xml+rss text/javascript application/x-javascript;
    gzip_types text/plain text/css text/xml application/json application/javascript
    gzip_comp_level 6;
    gzip_proxied any;
    gzip_vary on;
    gzip on;
    # Compression Gzip

    client_max_body_size 50M;
    types_hash_max_size 2048;
    keepalive_timeout 65;
    tcp_nodelay on;
    tcp_nopush on;
    sendfile on;
    # Optimisations

    access_log /var/log/nginx/access.log main;

                    'uht="$upstream_header_time" urt="$upstream_response_time"';
                    'rt=$request_time uct="$upstream_connect_time" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    '$status $body_bytes_sent "$http_referer" '
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
    # Format de log personnalisé

    default_type application/octet-stream;
    include /etc/nginx/mime.types;
http {

}
    multi_accept on;
    use epoll;
    worker_connections 1024;
events {

pid /var/run/nginx.pid;
error_log /var/log/nginx/error.log warn;
worker_processes auto;

# ===============================================
# Configuration Nginx pour reverse proxy
# nginx.conf - SmartLogi SDMS

